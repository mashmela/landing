import { style } from "@vanilla-extract/css";

export const notFoundContainerStyles = style({
  display: "flex",
  flexDirection: "column",
  gap: 16,
  flex: 1,
  justifyContent: "center",
  alignItems: "center",
});
